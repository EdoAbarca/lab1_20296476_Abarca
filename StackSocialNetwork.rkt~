#lang racket
;TDA stack social network
;Composicion: (UsuarioLogueado x UsuariosRegistrados x PubliacionesRealizadas x ReaccionesOcurridas)

;Constructor
;(define (StackRedSocial)(list null (crearListaUsuarios) (crearListaPublicaciones) (crearListaReacciones)))
;(define (StackVacio) (list null null null null))
;Selectores

;(define (getUsuarioLogueado stack)(car stack))
;(define (getListaUsuarios stack)(car stack))
;(define (getListaPublicaciones stack)(car stack))
;(define (getListaReacciones stack)(car stack))

;Pertenencia
;(define (StackRedSocial? stack)(cond[(not (list? stack))#f][(not (= (length stack) 4))#f][(not ())#f][(not ())#f][(not ())#f][(not ())#f][else #t]))

;Modificadores

;Otros

;AQUI SE ACTUALIZARA LA INFORMACION A OCURRIR EN LA RED SOCIAL, ESTARÁ EL ESPACIO PARA LOGUEO ACTUAL Y LOS TDA NECESARIOS